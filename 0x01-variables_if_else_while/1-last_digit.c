#include <stdio.h>
#include <time.h>
/**
 * main - prints "whether the number stored in tje variable is positive or negative"
 */
int main(void)
{
	int n;
	srand(time(0));
	n = rand() - RAND_MAX / 2

	if(n>0){
		printf("n is positive\n");
	}
	else if(n==o){
		printf("n is zero\n");
	}
	else {
		printf("n is negative\n");
	return (0)
	}
}
